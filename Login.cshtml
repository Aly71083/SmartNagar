@model SmartNagar.ViewModels.LoginVM
@{
    Layout = null;
    ViewData["Title"] = "Login";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Login | Smart Nagar</title>

    <style>
        * {
            box-sizing: border-box
        }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: "Segoe UI",system-ui,Arial;
            background: linear-gradient(135deg, #1ea4c8, #31b87b);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .card {
            width: 100%;
            max-width: 420px;
            background: #fff;
            border-radius: 18px;
            padding: 28px 26px 30px;
            box-shadow: 0 25px 60px rgba(0,0,0,.25);
        }

        .icon {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            background: #31b87b;
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 10px;
            font-size: 22px;
        }

        h2 {
            text-align: center;
            margin: 10px 0 4px;
            font-weight: 900;
            color: #0b1220;
        }

        .sub {
            text-align: center;
            color: #6a768a;
            font-size: 13px;
            margin-bottom: 20px;
        }

        label {
            font-size: 13px;
            font-weight: 700;
            color: #24324b;
            display: block;
            margin-bottom: 6px;
        }

        input {
            width: 100%;
            padding: 12px 14px;
            border-radius: 10px;
            border: 1px solid #dbe4f1;
            font-size: 14px;
            outline: none;
        }

            input:focus {
                border-color: #31b87b;
                box-shadow: 0 0 0 3px rgba(49,184,123,.15);
            }

        .field {
            margin-bottom: 14px;
        }

        .forgot {
            text-align: right;
            margin: 6px 0 14px;
        }

            .forgot a {
                font-size: 12px;
                color: #0b5cff;
                text-decoration: none;
                font-weight: 700;
            }

        .btn {
            width: 100%;
            padding: 12px;
            border-radius: 12px;
            border: none;
            font-weight: 900;
            font-size: 15px;
            cursor: pointer;
            background: #1ea4c8;
            color: #fff;
            box-shadow: 0 14px 30px rgba(30,164,200,.35);
        }

        .bottom {
            margin-top: 16px;
            text-align: center;
            font-size: 13px;
            color: #6a768a;
        }

            .bottom a {
                color: #1ea4c8;
                font-weight: 800;
                text-decoration: none;
            }

        .error {
            color: #d12f2f;
            font-size: 12px;
            margin-bottom: 10px;
            text-align: center;
        }
    </style>
</head>

<body>

    <form asp-action="Login" asp-controller="Account" method="post" class="card">

        <div class="icon">🏛️</div>

        <h2>Smart Nagar System</h2>
        <div class="sub">Login to access municipal services</div>

        <div class="error" asp-validation-summary="All"></div>

        <div class="field">
            <label>Email Address</label>
            <input asp-for="Email" placeholder="Enter your email" />
        </div>

        <div class="field">
            <label>Password</label>
            <input asp-for="Password" type="password" placeholder="Enter your password" />
        </div>

        <div class="forgot">
            <a href="#">Forgot Password?</a>
        </div>

        <button class="btn" type="submit">Login</button>

        <div class="bottom">
            Don’t have an account?
            <a href="/Account/Register">Register Now</a>
        </div>

    </form>

</body>
</html>
